global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "event-processor"
    static_configs:
      - targets:
          - "worker:9100"
          - "worker-2:9101"
        labels:
          service: "event-processor"
